<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://infj-clover.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://infj-clover.github.io/" rel="alternate" type="text/html" /><updated>2026-03-27T19:57:34+09:00</updated><id>https://infj-clover.github.io/feed.xml</id><title type="html">dev.clover</title><subtitle>dev transmissions</subtitle><author><name>infj-clover</name><email>infj-clover@gmail.com</email></author><entry><title type="html">OLTP vs OLAP: 데이터 아키텍처 설계의 핵심 차이</title><link href="https://infj-clover.github.io/posts/olap-oltp/" rel="alternate" type="text/html" title="OLTP vs OLAP: 데이터 아키텍처 설계의 핵심 차이" /><published>2026-03-24T00:00:00+09:00</published><updated>2026-03-24T00:00:00+09:00</updated><id>https://infj-clover.github.io/posts/olap-oltp</id><content type="html" xml:base="https://infj-clover.github.io/posts/olap-oltp/"><![CDATA[<h2 id="개요">개요</h2>

<p>데이터 엔지니어링의 핵심은 <strong>데이터를 효율적으로 이동(ETL)시키고 목적에 맞게 가공하는 것</strong>이다. 소스(Source) 시스템과 목적지(Sink)의 성격에 따라 데이터 모델링과 파이프라인 설계 전략은 완전히 달라진다.</p>

<p>데이터 아키텍처의 근간이 되는 <strong>OLTP</strong>와 <strong>OLAP</strong>의 기술적 차이를 정리한다.</p>

<hr />

<h2 id="1-개념-정의">1. 개념 정의</h2>

<h3 id="oltp-online-transactional-processing">OLTP (Online Transactional Processing)</h3>
<ul>
  <li><strong>목적:</strong> 실시간 서비스 운영 (결제, 주문, 회원 정보 관리 등)</li>
  <li><strong>핵심:</strong> 고빈도 저용량 데이터 처리. 데이터 무결성과 낮은 지연 시간(Low Latency)이 최우선이다.</li>
  <li><strong>주요 작업:</strong> 개별 레코드 단위의 CRUD(INSERT, UPDATE, DELETE).</li>
</ul>

<h3 id="olap-online-analytical-processing">OLAP (Online Analytical Processing)</h3>
<ul>
  <li><strong>목적:</strong> 의사결정을 위한 데이터 분석 (매출 지표 분석, 사용자 행동 패턴 추출 등)</li>
  <li><strong>핵심:</strong> 저빈도 대용량 데이터 조회. 대량의 데이터를 처리하는 처리량(Throughput)과 복잡한 쿼리 성능이 최우선이다.</li>
  <li><strong>주요 작업:</strong> 대규모 집계 및 조회를 수행하는 SELECT.</li>
</ul>

<hr />

<h2 id="2-기술적-특성-비교">2. 기술적 특성 비교</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>OLTP (Transaction)</th>
      <th>OLAP (Analytics)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>데이터 범위</strong></td>
      <td>현재 시점의 최신 데이터</td>
      <td>과거부터 축적된 이력 데이터</td>
    </tr>
    <tr>
      <td><strong>저장 방식</strong></td>
      <td>행 기반 (Row-oriented)</td>
      <td>열 기반 (Columnar)</td>
    </tr>
    <tr>
      <td><strong>모델링</strong></td>
      <td>3차 정규화 (중복 제거)</td>
      <td>비정규화 (Join 비용 최적화)</td>
    </tr>
    <tr>
      <td><strong>주요 병목</strong></td>
      <td>Lock 경합, 인덱스 오버헤드</td>
      <td>I/O 대역폭, CPU 연산량</td>
    </tr>
    <tr>
      <td><strong>대표 도구</strong></td>
      <td>MySQL, PostgreSQL, Oracle</td>
      <td>BigQuery, Snowflake, Redshift</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="3-데이터-엔지니어링-관점의-설계-전략">3. 데이터 엔지니어링 관점의 설계 전략</h2>

<p>데이터 엔지니어는 이 두 환경 사이의 기술적 간극을 메우는 인프라를 설계한다.</p>

<ol>
  <li><strong>데이터 추출(Extraction):</strong> 운영 DB에 부하를 주지 않기 위해 Read-Replica를 사용하거나, 로그 기반의 CDC(Change Data Capture)를 적용해 변경 데이터를 실시간으로 수집한다.</li>
  <li><strong>데이터 적재(Loading):</strong> 대규모 조회가 원활하도록 데이터 웨어하우스의 특성에 맞춰 파티셔닝(Partitioning)과 클러스터링(Clustering)을 최적화한다.</li>
</ol>

<blockquote>
  <p><strong>Key Point:</strong> OLTP는 <strong>정확한 기록</strong>에, OLAP은 <strong>빠른 읽기</strong>에 최적화된 구조를 가진다.</p>
</blockquote>

<hr />

<h2 id="4-열-기반columnar-저장-방식의-이점">4. 열 기반(Columnar) 저장 방식의 이점</h2>

<p>분석 쿼리는 대개 특정 컬럼의 집계(<code class="language-plaintext highlighter-rouge">SUM</code>, <code class="language-plaintext highlighter-rouge">AVG</code> 등)를 목적으로 한다.</p>

<ul>
  <li><strong>I/O 효율성:</strong> 행 기반 저장소와 달리 필요한 컬럼만 디스크에서 읽어오므로 불필요한 데이터 로드가 없다.</li>
  <li><strong>압축률:</strong> 동일한 데이터 타입이 연속적으로 저장되므로 압축 효율이 극대화된다. 이는 저장 공간 절약과 더불어 I/O 성능 향상으로 직결된다.</li>
</ul>

<hr />

<h2 id="결론">결론</h2>

<p>비즈니스 요구사항에 따라 적합한 아키텍처를 선택하는 것은 데이터 엔지니어의 필수 역량이다. 운영 환경(OLTP)의 트랜잭션 데이터를 통합하여 분석 환경(OLAP)에서 가치를 창출할 수 있는 구조를 설계하는 것이 파이프라인 구축의 본질적인 목표다.</p>

<hr />

<h2 id="reference">Reference</h2>
<ul>
  <li>데이터 중심 애플리케이션 설계 (DDIA) - 3장 저장소와 검색</li>
  <li>AWS Documentation: OLTP와 OLAP 비교</li>
</ul>]]></content><author><name>infj-clover</name><email>infj-clover@gmail.com</email></author><category term="til" /><category term="data-engineering" /><category term="database" /><category term="olap" /><category term="oltp" /><summary type="html"><![CDATA[운영 환경과 분석 환경의 기술적 차이. 효율적인 데이터 파이프라인 설계를 위해 반드시 구분해야 할 OLTP와 OLAP의 특성을 정리합니다.]]></summary></entry><entry><title type="html">Claude Code로 GitHub.io 블로그 제작</title><link href="https://infj-clover.github.io/posts/claude-code-init/" rel="alternate" type="text/html" title="Claude Code로 GitHub.io 블로그 제작" /><published>2026-03-23T00:00:00+09:00</published><updated>2026-03-23T00:00:00+09:00</updated><id>https://infj-clover.github.io/posts/claude-code-init</id><content type="html" xml:base="https://infj-clover.github.io/posts/claude-code-init/"><![CDATA[<h2 id="시작--왜-직접-만들었나">시작 — 왜 직접 만들었나</h2>

<p>처음엔 그냥 Jekyll 테마 하나 골라서 쓰려고 했다. Chirpy, jekyll-theme-console 같은 SF 감성 테마들을 살펴봤는데, 뭔가 내 것이 아닌 느낌이었다. 그래서 Claude에게 직접 만들어 달라고 했다.</p>

<p>결과적으로 테마 선택 → 디자인 → 코드 → 배포까지 전 과정을 Claude와 함께 진행했고, 예상보다 훨씬 빠르게 완성됐다.</p>

<hr />

<h2 id="1단계--프레임워크-선택">1단계 — 프레임워크 선택</h2>

<p>Jekyll, Hugo, Gatsby 세 가지를 비교했다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>Jekyll</th>
      <th>Hugo</th>
      <th>Gatsby</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>언어</td>
      <td>Ruby</td>
      <td>Go</td>
      <td>React/JS</td>
    </tr>
    <tr>
      <td>빌드속도</td>
      <td>느림</td>
      <td>초고속</td>
      <td>보통</td>
    </tr>
    <tr>
      <td>GH Pages</td>
      <td>네이티브</td>
      <td>Actions필요</td>
      <td>Actions필요</td>
    </tr>
    <tr>
      <td>난이도</td>
      <td>쉬움</td>
      <td>보통</td>
      <td>어려움</td>
    </tr>
  </tbody>
</table>

<p>GitHub Pages에 네이티브로 연동되고 난이도가 낮은 <strong>Jekyll</strong>을 선택했다. 처음이라면 Jekyll + Chirpy 조합이 레퍼런스가 많아 추천된다고 하는데, 나는 처음부터 커스텀으로 가기로 했다.</p>

<hr />

<h2 id="2단계--디자인-방향-설정">2단계 — 디자인 방향 설정</h2>

<p>원하는 느낌을 Claude에게 전달했다.</p>

<ul>
  <li><strong>사이버펑크 SF</strong> 감성</li>
  <li>분홍색 계열 완전 배제</li>
  <li>단일 액센트 컬러 (시안)</li>
  <li>유리 패널 없이 불투명 단색</li>
  <li>우주에서 블로그를 보는 느낌</li>
</ul>

<p>몇 차례 피드백을 주고받으며 디자인을 다듬었다. 처음엔 색상이 너무 많고 난잡했는데, 시안 단색으로 정리하고 나서야 원하는 느낌이 나왔다.</p>

<p>최종적으로 적용된 효과들은 다음과 같다.</p>

<ul>
  <li>페이지 로드 시 스캔라인 부팅 플래시</li>
  <li>성운 파티클 배경 (Canvas API)</li>
  <li>카드 호버 shimmer sweep</li>
  <li>라이트 모드 도트 그리드 배경</li>
</ul>

<hr />

<h2 id="3단계--jekyll-구조로-컴포넌트-분리">3단계 — Jekyll 구조로 컴포넌트 분리</h2>

<p>단일 HTML 파일로 완성된 데모를 Jekyll 프로젝트 구조로 분리했다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>void-log/
├── _layouts/
│   ├── default.html     # 공통 레이아웃
│   └── post.html        # 포스트 레이아웃
├── _includes/
│   ├── header.html      # 헤더 컴포넌트
│   ├── sidebar.html     # 사이드바 컴포넌트
│   └── post-card.html   # 포스트 카드 컴포넌트
├── _sass/
│   ├── _tokens.scss     # CSS 변수 (색상, 폰트)
│   ├── _background.scss # 우주 배경 레이어
│   └── pages/           # 페이지별 스타일
├── _data/
│   ├── projects.yml     # 프로젝트 데이터
│   └── skills.yml       # 기술 스택 데이터
├── _posts/              # 블로그 포스트
└── assets/js/
    ├── theme.js         # 다크/라이트 토글
    ├── clock.js         # 실시간 시계
    └── nebula.js        # 성운 파티클
</code></pre></div></div>

<p>CSS는 SASS로 분리해서 <code class="language-plaintext highlighter-rouge">_tokens.scss</code>에 CSS 변수를 몰아넣었다. 나중에 색상을 바꾸고 싶으면 <code class="language-plaintext highlighter-rouge">--c-accent</code> 값 하나만 수정하면 전체에 반영된다.</p>

<hr />

<h2 id="4단계--로컬-환경-세팅">4단계 — 로컬 환경 세팅</h2>

<p>macOS 기본 Ruby가 2.6이라 Jekyll 4가 설치되지 않았다. <code class="language-plaintext highlighter-rouge">rbenv</code>로 버전을 올려야 했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># rbenv 설치</span>
brew <span class="nb">install </span>rbenv ruby-build

<span class="c"># 쉘 설정</span>
<span class="nb">echo</span> <span class="s1">'eval "$(rbenv init -)"'</span> <span class="o">&gt;&gt;</span> ~/.zshrc
<span class="nb">eval</span> <span class="s2">"</span><span class="si">$(</span>rbenv init -<span class="si">)</span><span class="s2">"</span>

<span class="c"># Ruby 3.2 설치 (2~3분 소요)</span>
rbenv <span class="nb">install </span>3.2.2
rbenv global 3.2.2

<span class="c"># 버전 확인</span>
ruby <span class="nt">-v</span>  <span class="c"># ruby 3.2.2 가 나와야 함</span>
</code></pre></div></div>

<p>버전 확인 후 의존성 설치.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gem <span class="nb">install </span>bundler
bundle <span class="nb">install</span>
</code></pre></div></div>

<p>로컬 서버 실행.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bundle <span class="nb">exec </span>jekyll serve
<span class="c"># http://localhost:4000 에서 확인</span>
</code></pre></div></div>

<hr />

<h2 id="5단계--github-pages-배포">5단계 — GitHub Pages 배포</h2>

<p><code class="language-plaintext highlighter-rouge">github-pages</code> gem을 쓰면 Jekyll 1.x를 강제해서 버전 충돌이 난다. GitHub Actions로 직접 빌드하는 방식을 사용했다.</p>

<p><code class="language-plaintext highlighter-rouge">.github/workflows/deploy.yml</code> 파일을 추가하면 push할 때마다 자동 빌드 &amp; 배포가 된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git init
git remote add origin https://github.com/infj-clover/infj-clover.github.io.git
git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"init: void.log jekyll blog"</span>
git push <span class="nt">-u</span> origin main
</code></pre></div></div>

<p>GitHub 리포 → <strong>Settings → Pages → Source → GitHub Actions</strong> 로 변경하면 완료.</p>

<p>1~2분 후 <code class="language-plaintext highlighter-rouge">https://infj-clover.github.io</code> 에서 확인할 수 있다.</p>

<hr />

<h2 id="이후-포스트-작성-방법">이후 포스트 작성 방법</h2>

<p><code class="language-plaintext highlighter-rouge">_posts/</code> 폴더에 <code class="language-plaintext highlighter-rouge">YYYY-MM-DD-제목.md</code> 형식으로 파일을 만들면 된다.</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">layout</span><span class="pi">:</span> <span class="s">post</span>
<span class="na">title</span><span class="pi">:</span> <span class="s2">"</span><span class="s">포스트</span><span class="nv"> </span><span class="s">제목"</span>
<span class="na">date</span><span class="pi">:</span> <span class="s">2025-04-01</span>
<span class="na">tags</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">rust</span><span class="pi">,</span> <span class="nv">linux</span><span class="pi">]</span>
<span class="na">read_time</span><span class="pi">:</span> <span class="m">10</span>
<span class="nn">---</span>

본문 내용...
</code></pre></div></div>

<p>저장 후 push하면 자동 배포된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"post: 포스트 제목"</span>
git push
</code></pre></div></div>

<hr />

<h2 id="정리">정리</h2>

<p>Claude에게 원하는 느낌을 말로 설명하면서 디자인을 다듬어 나가는 과정이 꽤 재밌었다. 테마 고르고 설정하는 데 쓸 시간을 아끼고, 원하는 결과물을 더 빠르게 얻을 수 있었다.</p>

<p>과거에 백엔드 공부할 때 2-3일 정도 시도하다가 Jekyll 템플릿이 마음에 들지 않아 포기했던 경험이 있는데 이번에 클로드 코드로 테마를 직접 제작하고 배포 하는데 까지 30분 만에 끝났다. 제작한 김에 앞으로 블로그 플랫폼을 이쪽으로 옮겨볼 계획이다.</p>]]></content><author><name>infj-clover</name><email>infj-clover@gmail.com</email></author><category term="til" /><category term="jekyll" /><category term="github-pages" /><category term="blog" /><category term="claude" /><summary type="html"><![CDATA[Claude Code로 커스텀 SF 디자인 제작, GitHub Pages 배포까지 — Claude와 함께한 블로그 구축 과정 전체 기록.]]></summary></entry><entry><title type="html">Rust로 제로 코스트 추상화 구현하기 — 실전 트레이트 패턴</title><link href="https://infj-clover.github.io/posts/rust-zero-cost-abstraction/" rel="alternate" type="text/html" title="Rust로 제로 코스트 추상화 구현하기 — 실전 트레이트 패턴" /><published>2025-03-20T00:00:00+09:00</published><updated>2025-03-20T00:00:00+09:00</updated><id>https://infj-clover.github.io/posts/rust-zero-cost-abstraction</id><content type="html" xml:base="https://infj-clover.github.io/posts/rust-zero-cost-abstraction/"><![CDATA[<h2 id="개요">개요</h2>

<p>Rust의 가장 강력한 특징 중 하나는 <strong>제로 코스트 추상화(zero-cost abstraction)</strong>입니다.
추상화를 사용해도 런타임 비용이 없다는 의미인데, 이게 실제로 어떻게 동작하는지 살펴봅니다.</p>

<h2 id="트레이트-기반-정적-디스패치">트레이트 기반 정적 디스패치</h2>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Zero-cost abstraction via static dispatch</span>
<span class="k">trait</span> <span class="n">Processor</span> <span class="p">{</span>
    <span class="k">fn</span> <span class="nf">process</span><span class="p">(</span><span class="o">&amp;</span><span class="k">self</span><span class="p">,</span> <span class="n">data</span><span class="p">:</span> <span class="o">&amp;</span><span class="p">[</span><span class="nb">u8</span><span class="p">])</span> <span class="k">-&gt;</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">u8</span><span class="o">&gt;</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">fn</span> <span class="n">run</span><span class="o">&lt;</span><span class="n">T</span><span class="p">:</span> <span class="n">Processor</span><span class="o">&gt;</span><span class="p">(</span><span class="n">p</span><span class="p">:</span> <span class="o">&amp;</span><span class="n">T</span><span class="p">,</span> <span class="n">payload</span><span class="p">:</span> <span class="o">&amp;</span><span class="p">[</span><span class="nb">u8</span><span class="p">])</span> <span class="p">{</span>
    <span class="k">let</span> <span class="n">result</span> <span class="o">=</span> <span class="n">p</span><span class="nf">.process</span><span class="p">(</span><span class="n">payload</span><span class="p">);</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"output: {:?}"</span><span class="p">,</span> <span class="n">result</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">T: Processor</code>처럼 제네릭 경계를 사용하면 컴파일러가 각 타입에 맞춰
함수를 <strong>단형화(monomorphization)</strong>합니다. 가상 테이블(vtable) 없이
직접 호출로 컴파일되므로 오버헤드가 0입니다.</p>

<h2 id="동적-디스패치와의-비교">동적 디스패치와의 비교</h2>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>런타임 비용</th>
      <th>유연성</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">T: Processor</code> (정적)</td>
      <td>없음</td>
      <td>컴파일 타임 고정</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">dyn Processor</code> (동적)</td>
      <td>vtable 조회</td>
      <td>런타임 다형성</td>
    </tr>
  </tbody>
</table>

<p>실제 측정 결과 정적 디스패치는 <code class="language-plaintext highlighter-rouge">dyn Trait</code> 대비 약 <strong>15~30% 빠른</strong> 결과를 보입니다.</p>

<h2 id="결론">결론</h2>

<p>추상화 레이어가 필요하다면 가능한 한 제네릭을 활용하고,
런타임 다형성이 꼭 필요한 경우에만 <code class="language-plaintext highlighter-rouge">dyn Trait</code>을 사용하세요.</p>]]></content><author><name>infj-clover</name><email>infj-clover@gmail.com</email></author><category term="til" /><category term="rust" /><category term="systems" /><category term="performance" /><summary type="html"><![CDATA[컴파일 타임 다형성과 제네릭 경계를 활용해 런타임 오버헤드 없이 유연한 추상화 레이어를 설계하는 방법.]]></summary></entry></feed>